#include "main.h"
#include "stdio.h"
/**
 * main - check the code
 *
 * Return: Always 0.
 */

void print_alphabet(char c);

int main(void)
{
   	print_alphabet(char c);
    return (0);
}

void print_alphabet(void){

	char c;
	for ( i = 'a'; i <= 'z'; i += 1)
	{
	_putchar(i);
	}
	_putchar('\n');
}
