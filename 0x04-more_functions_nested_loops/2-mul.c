#include "main.h"
#include <stdio.h>

/**
 * _isdigit - Check if paramter c represents an digit.
 * @a: first integer
 * @b: second integer
 * Return: Return 1 if it is a digit otherwise return 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
