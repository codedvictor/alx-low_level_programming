#include "main.h"
/**
 * mul - returns the product of its parameters
 * @a: printed number
 * Return:0
 */

void print_numbers(void)
{
	int a;

	for (a = 0; a <= 9; a += 1)
	{
		_putchar (a);
	}
	_putchar('\n');
}
