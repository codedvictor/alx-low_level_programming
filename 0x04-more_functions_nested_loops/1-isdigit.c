#include "main.h"
#include <stdio.h>

/**
 * _isdigit - main entry
 * @c: is the number to check
 * Return: 1 success
 */

int _isdigit(int c)
{
	c = 0;
	while (c <= 9)
	{
		return (1);
		
		if (c < 0 || c >9)
		{
			return (0);
		}
	}

}
