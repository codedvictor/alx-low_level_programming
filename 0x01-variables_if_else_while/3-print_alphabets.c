#include <stdio.h>

int main(void)
{
	char alpha;
	     for (alpha = 'a'; alpha <= 'Z'; alpha +=1)
		     putchar(alpha);
	/*for (beta = 'A'; beta <- 'Z'; beta +=1)
		putchar(beta);*/
	putchar('\n');

	return (0);
}
